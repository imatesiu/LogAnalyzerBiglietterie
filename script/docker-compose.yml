services:
  viewer:
    build: .
    expose:
      - "5000"
    volumes:
      - ./app/xsd:/app/xsd:ro
      - ./app/uploads:/app/uploads
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  reverse-proxy:
    image: nginx:1.27-alpine
    depends_on:
      - viewer
    ports:
      - "8443:443"
    volumes:
      - ./app/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./app/tls/fullchain.pem:/etc/nginx/certs/fullchain.pem:ro
      - ./app/tls/privkey.pem:/etc/nginx/certs/privkey.pem:ro
    restart: unless-stopped
